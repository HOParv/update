import subprocess
import os

print("welcome tu linux auto-updater, please always run has sudo")

while true:
	if os.getuid != else:
		print("execute has sudo using sudo python3")
		break
	if os.getuid != 0
		
